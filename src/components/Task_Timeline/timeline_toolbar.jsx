import React from 'react'

const TimelineToolbar = ({ onAddTask }) => {
	return (
		<div className='px-4 py-3 border-t border-gray-700 flex items-center'>
			{/* Removed the + New button as requested */}
		</div>
	)
}

export default TimelineToolbar